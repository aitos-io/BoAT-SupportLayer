# Select one of the hardware

all:
	make -C $(BOAT_BASE_DIR)/BoAT-SupportLayer/vendor/platform/$(PLATFORM_TARGET) all

clean:	
	make -C $(BOAT_BASE_DIR)/BoAT-SupportLayer/vendor/platform/$(PLATFORM_TARGET) clean
